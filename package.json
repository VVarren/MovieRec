{
	"name": "your-repo",
	"private": true,
	"scripts": {
		"dev:web": "pnpm --filter @yourorg/web dev",
		"dev:api": "cd apps/api && uvicorn main:app --reload",
		"dev": "concurrently \"pnpm dev:web\" \"pnpm dev:api\""
	},
	"devDependencies": {
		"concurrently": "^9.2.1"
	}
}
